'use strict';

const { collector } = require('../../e2e/utils/page.collector');
const path = require('path');

describe('Unit tests of collector functionality', () => {
    it('Result json shood be equals expected json', () => {
        collector.
            console.log(path.resolve('.'));
    });
});